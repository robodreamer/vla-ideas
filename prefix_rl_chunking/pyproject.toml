[project]
name = "prefix-rl-chunking"
version = "0.1.0"
description = "Toy PPO + action-prefix loss demo for chunked VLA policies"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "matplotlib>=3.8",
    "numpy>=1.26",
    "torch>=2.0",
]

[tool.uv]
package = false
